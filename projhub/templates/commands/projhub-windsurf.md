---
description: Initialize or inspect projhub in the current project directory
---

Steps:
1. Run `projhub doctor` to check if `.projhub/` is already initialized.
2. If not initialized: run `projhub init`, then `projhub compile --target windsurf`.
3. If already initialized: run `projhub board ls` and show the board status.

Show all command output to the user.
